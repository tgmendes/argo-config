apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: env1
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/tgmendes/argo-config.git
        revision: HEAD
        directories:
          - path: nonprod/env1/services/*
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: env1
      source:
        repoURL: https://github.com/tgmendes/argo-config.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: env1
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
